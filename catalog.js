export const esc=value=>String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function card(p,i,base='../'){return `<article class="product-card"><div class="product-heading"><h3>${esc(p.name)}</h3><span class="product-index">${String(i+1).padStart(2,'0')}</span></div><a class="product-image" href="${base}products/${p.slug}/"><img src="${base}${p.image}" alt="${esc(p.name)} interface" loading="lazy"></a><div class="product-info"><p>${esc(p.category)}</p><span class="technical">${esc(p.status)} / ${esc(p.platform)}</span><p>${esc(p.shortDescription)}</p><a class="text-link" href="${base}products/${p.slug}/">Explore machine ⟶</a></div></article>`}
