import { products } from "./data/products.js";const root=document.querySelector("#product-grid");const details=new Set(["timefield","avm"]);root.innerHTML=products.map((product,index)=>{const href=product.appUrl?`../${product.appUrl}`:details.has(product.slug)?`../products/${product.slug}/`:"#";const linked=href!=="#";return `<article class="product-card reveal"><div class="product-heading"><h3>${product.name}</h3></div><${linked?"a":"div"} class="product-image" ${linked?`href="${href}"`:""}><img loading="lazy" src="../${product.image}" alt="${product.name} interface"><span class="index">${String(index+1).padStart(2,"0")}</span></${linked?"a":"div"}><div class="product-info"><p>${product.category}</p><span class="technical">${product.exhibitNotes}</span>${linked?`<a class="view-link" href="${href}">VIEW →</a>`:`<span class="view-link muted-link">DETAIL PAGE NEEDED</span>`}</div></article>`}).join("");const toggle=document.querySelector(".nav-toggle");toggle.addEventListener("click",()=>{const open=toggle.getAttribute("aria-expanded")==="true";toggle.setAttribute("aria-expanded",String(!open));document.querySelector("#nav").classList.toggle("open",!open)});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>entry.isIntersecting&&entry.target.classList.add("visible")),{threshold:.08});document.querySelectorAll(".reveal").forEach(element=>observer.observe(element));
